# Decisions — build-checkpoint-reader

| id | question | class | resolution | evidence |
|----|----------|-------|------------|----------|
| D1 | How is the reader app itself gated: a shared access key exchanged for an httpOnly session cookie, Vercel Deployment Protection, or Cloudflare Access? | needs_human | — | — |
| D2 | Which git host does the adapter target in production? | agent_resolved | GitHub; the adapter contract stays host-neutral | `git -C ~/IdeaProjects/specflow remote -v` and project-wedding both on github.com/renzrollon |
| D3 | Where does the approval marker live: `.claude/checkpoint/<change>.json` or the change directory? | agent_resolved | `openspec/changes/<change>/checkpoint.json`; a marker the laptop side does not find reads as not approved | app-ideas brief `top-5-app-ideas-2026-09-05.md:86` (prose) over `:80` (sketch); explore brief §Assumptions Made |
| D4 | Does the reader talk to ntfy or implement web push? | agent_resolved | Neither; the ntfy phone app opens the click URL and the reader is only the target | app-ideas brief `:76-79,84`; explore brief §Options Considered item 4 |
| D5 | Is there a database or server-side cache in v0.1? | agent_resolved | No; every read is a git host read on a ref and every write is a commit | app-ideas brief `:85` |
| D6 | Which versions are pinned for the stack and the markdown renderer? | agent_resolved | next 16.3.1, react 19.2.8, typescript 5.9.3, vitest 4.1.10, @playwright/test 1.62.1, tailwindcss 4.3.3, zod 4.4.3 and the rest of the design D6 table; react-markdown 10.1.0, remark-gfm 4.0.1 | `~/IdeaProjects/trading-system/web/package.json`; `npm view react-markdown version` and `npm view remark-gfm version` on 2026-09-05 |
| D7 | Import specflow's ledger module at runtime or port it? | agent_resolved | Port `parseLedger`, `serializeLedger`, `isEmptyCell`, `summarize` to TypeScript and pin parity with golden fixtures generated from the real module | `~/IdeaProjects/specflow/lib/ledger.mjs:161-297`; explore brief §Options Considered item 3 |
| D8 | May the reader record an approval while the ledger still blocks? | agent_resolved | No; Approve is disabled with the reason and the server action refuses too; Send back stays available | `~/IdeaProjects/specflow/lib/ready.mjs:341-388` |
| D9 | What does the sender write, and what happens when it has written nothing? | agent_resolved | Sender is assumed to write a `request` block into `checkpoint.json`; the reader tolerates its absence, shows risk as unobserved, and always computes counts itself | app-ideas brief `:61` and constraint `:30`; design D9 JSON contract |
| D10 | How are concurrent laptop and phone edits handled? | agent_resolved | Writes carry the blob SHA shown to the owner; the GitHub contents API rejects a stale SHA and the reader reloads without retrying | GitHub REST docs, `PUT /repos/{owner}/{repo}/contents/{path}` requires `sha` on update; `lib/ledger.mjs` has no merge semantics |
| D11 | Where is the reader hosted? | agent_resolved | Vercel, documented in README only; no Vercel-specific code | app-ideas brief `:18` (project-wedding on Vercel) and `:85` |
| D12 | Does the reader render ship halts or run trajectories? | agent_resolved | No; trajectories live on the laptop, not on the branch, so a halt push carries its own text | `~/IdeaProjects/specflow/docs/04-when-it-stops.md:57` |
| D13 | What evidence text is written when a human answers a row from the phone? | agent_resolved | `human decision <YYYY-MM-DD> via Checkpoint`, date in UTC | `~/.claude/plugins/cache/interlock/interlock/0.2.0/shared/DECISION-LEDGER.md:71-74` |
| D14 | Where do the one-tap options for a `needs_human` row come from? | agent_resolved | Parsed from the question text on " or ", " vs ", " versus ", " / "; at most four; free text always available | app-ideas brief `:63`; `lib/ledger.mjs:42` shows the table has no options column |
| D15 | What commit messages and author do phone-side writes use? | agent_resolved | `checkpoint(<change>): answer <id>`, `checkpoint(<change>): approve`, `checkpoint(<change>): return`; author is the token owner | app-ideas brief `:63` (commit through the git host API) |
| D16 | How are server behaviours tested without a token or network? | agent_resolved | A fixture adapter over `fixtures/repo/` with an in-memory write overlay implements the same contract and is selected by `CHECKPOINT_GIT_HOST=fixture` | design §Module layout; trading-system web mock mode precedent in app-ideas brief `:234` |
| D17 | What may the reader write, and with what token scope? | agent_resolved | Only `decisions.md` and `checkpoint.json` under `openspec/changes/<change>/`, guarded before any request; fine-grained token with Contents read and write on one repository | app-ideas brief `:69` and `:96` |
| D18 | How is the branch chosen? | agent_resolved | `ref` query parameter, default branch when absent; header always shows ref and short head | app-ideas brief `:62` (rendered from the branch on the remote) |
| D19 | Does the PWA ship a service worker? | agent_resolved | No; manifest and icons only, offline is a non-goal | proposal.md "Not in this change: offline caching" |
| D20 | Are task, wave and ledger counts taken from the sender or computed? | agent_resolved | Always computed from `tasks.md` and `decisions.md` on the ref with Interlock's regexes | `~/IdeaProjects/specflow/lib/artifacts.mjs:79-85`; app-ideas brief constraint `:30` |
| D21 | Where does the visual design come from: implementer judgement, or a Claude Design comp briefed from the repo? | agent_resolved | A Claude Design brief is written in this change (task 1.8); the comp is a human step at the checkpoint imported via `/design-comp-import`; screens implement from the transcription when present, else from the brief | user request 2026-09-05; `~/.claude/skills/design-comp-import/SKILL.md:24-38` (brief, `docs/design/`, transcription paths); `~/IdeaProjects/specflow/Interlock Report Design/` comps as the token source |
