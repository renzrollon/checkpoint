## 1. Foundation: scaffold, pure modules, adapter, handbook

- [x] 1.1 Scaffold the Next.js 16 app with the pinned versions from design D6: `package.json` (scripts dev, build, start, lint, typecheck, test, test:e2e; `engines.node >=24`), `tsconfig.json`, `next.config.ts`, `postcss.config.mjs`, `vitest.config.ts` (jsdom, React plugin, `src/**/*.test.{ts,tsx}`), `playwright.config.ts` (webServer started with `CHECKPOINT_GIT_HOST=fixture`), `.gitignore`, `.env.example`, `src/app/layout.tsx`, `src/app/globals.css`, `src/app/manifest.ts` and `public/icons/*` (design D19), and `src/lib/env.ts` validating `CHECKPOINT_ACCESS_KEY`, `CHECKPOINT_GITHUB_TOKEN`, `CHECKPOINT_REPO`, `CHECKPOINT_GIT_HOST`, `CHECKPOINT_FIXTURE_DIR` with zod, with `src/lib/env.test.ts` asserting a missing variable fails naming it. Acceptance: `npm run typecheck`, `npm test` and `npm run build` pass on the empty app.
- [x] 1.2 Port the ledger grammar (design D7) into `src/lib/ledger/ledger.ts` exporting `parseLedger`, `serializeLedger`, `isEmptyCell`, `summarize` and `answerRow` (which splices one re-rendered row at its parsed line and leaves every other line byte-identical); add hand-written fixtures `fixtures/ledger/{basic,escaped-pipe,hedges,unparseable,missing-columns}.md`; add `scripts/gen-ledger-fixtures.mjs` that imports Interlock's `lib/ledger.mjs` from `INTERLOCK_LIB_DIR` and writes `.parsed.json` and `.serialized.md` golden files plus `fixtures/ledger/VERSION`; run it once and commit the goldens; write `src/lib/ledger/ledger.test.ts` asserting golden fixtures round-trip byte-identically, an escaped pipe survives an edit, hedge cells read as empty, an unparseable ledger is reported not emptied, `answerRow` produces the exact D1 row from the ledger-answering spec with every other line unchanged, and an answer containing a pipe is escaped. Depends on 1.1 for the test runner.
- [x] 1.3 Write `src/lib/tasks/tasks.ts` (design D20) exporting `parseTasks` returning total, done, waves counted from `## N.` headings, and per-task id, text and line using Interlock's checkbox and id regex; write `src/lib/tasks/tasks.test.ts` covering "4 of 12 tasks, 3 waves", a file with no section headings, upper-case `[X]`, and bullets that are not checkboxes. Depends on 1.1.
- [x] 1.4 Write `src/lib/checkpoint/checkpoint.ts` (design D9) with a zod passthrough schema for `interlock.checkpoint/1`, `readCheckpoint(text | null)` tolerant of an absent file and of an unknown risk class, `withDecision(existing, decision)` preserving `request` and unknown top-level fields, `riskView(checkpoint, headSha)` returning observed, stale or unobserved with class and recorded head, and `decisionView(checkpoint, headSha)` with a stale flag; write `src/lib/checkpoint/checkpoint.test.ts` covering recorded risk shown with its head, risk recorded for an older head, no recorded risk, unknown fields surviving a decision write, and a decision replacing a previous one. Depends on 1.1.
- [x] 1.5 Write `src/lib/ledger/options.ts` (design D14) exporting `optionsFromQuestion`; write `src/lib/ledger/options.test.ts` covering " or ", " vs ", " versus ", " / ", a colon-prefixed comma list, a trailing question mark, more than four candidates yielding none, and fragments under two characters dropped. Depends on 1.1.
- [x] 1.6 Build the git host adapter (design D10, D16, D17): `src/lib/githost/types.ts` (contract `resolveRef`, `readFile`, `listDir`, `writeFile`), `src/lib/githost/errors.ts` (`HostError` union with kinds unauthorized, forbidden with `retryAt`, not_found, conflict, forbidden_path, upstream), `src/lib/githost/guard.ts` (`guardWritePath` allowing only `openspec/changes/<change>/decisions.md` and `checkpoint.json`), `src/lib/githost/github.ts` (contents API with base64 decode, `PUT` with `sha`, mapping 401, 403 with `x-ratelimit-reset`, 404, 409, 422 and 5xx or network failure, token never in messages, default branch from the repository endpoint), `src/lib/githost/fixture.ts` (serves `fixtures/repo/`, git blob sha1, in-memory write overlay, head sha derived from the overlay), `src/lib/githost/index.ts` selecting by `CHECKPOINT_GIT_HOST`; create `fixtures/repo/openspec/changes/{add-user-auth,add-report-flag,archive/2026-08-01-old-change}` each with proposal, design, tasks, specs and decisions files, `add-user-auth` carrying one `needs_human` row and `add-report-flag` carrying a clear ledger and an approved `checkpoint.json`; write `src/lib/githost/contract.test.ts` (shared read, write, conflict, listing and path-guard scenarios run against the fixture adapter, including write-then-read on the same ref leaving disk unchanged) and `src/lib/githost/github.test.ts` (mocked `fetch` for every failure kind, the rate-limit reset time, the default branch, and the guarded write refused before any request). Depends on 1.1.
- [x] 1.7 Write `README.md`: purpose, environment variables, GitHub fine-grained token scope (one repository, Contents read and write), running locally with the fixture adapter, Vercel deployment (design D11), the `checkpoint.json` contract quoted verbatim from design D9, the ledger fixture regeneration command, and the open decision D1. Depends on 1.1 for the script names.
- [x] 1.8 Write the Claude Design brief `docs/checkpoint-design-brief.md` (design D21), the prompt to paste into Claude Design: product context in five lines (one owner, one decision, phone at 375 px, reads a branch, writes two files); the six screens with every state named in the specs and their exact copy strings quoted from `specs/*/spec.md` (login with "That key was not accepted" and the 503 text; inbox with "N needs you" / "clear" / "ledger missing", "No active changes on <ref>", and the host failure card; change page header with "N of M tasks", "N waves", "N needs you, N resolved, N invalid, of N rows", risk observed, "stale: recorded for <sha7>" and "risk unobserved"; artifact tabs Proposal, Design, Tasks, Specs, Ledger with the "<file> is missing on <ref>" state; ledger panel and answer form with option chips, "An answer is required" and the reloaded-conflict message; decision bar with the disabled-Approve reasons, "Say what should change", the stale label and the conflict message); design constraints as a checklist (every number shows its denominator, `unobserved` and `stale` are first-class states with the same visual authority as a value, text labels carry state and colour never carries it alone, tabs are a `role="tablist"`, focus order and 44 px touch targets, `prefers-reduced-motion` respected, light and dark); the tokens and the absence treatment to reuse from `~/IdeaProjects/specflow/Interlock Report Design/Interlock Report v2.dc.html` and `Indicator Variants.dc.html` so Checkpoint reads as Interlock's phone; a numbered acceptance list mirroring the spec scenarios that involve a screen; and a closing handoff paragraph stating the comp is a human step at the checkpoint that comes back into the repo through `/design-comp-import` as `docs/design/checkpoint.dc.html` plus `docs/checkpoint-design-spec.md`. Acceptance: the brief names every copy string the section 3 tests assert on, and nothing in it contradicts a spec scenario.

## 2. Server layer

- [x] 2.1 Implement the access gate (design D1 default): `src/lib/auth/session.ts` (HMAC-SHA256 tag over `v1|<expiry>` keyed by the access key, `timingSafeEqual` compare, 30-day expiry), `src/proxy.ts` (Next.js 16 request interceptor: redirect page requests to `/login?next=<path>`, respond 401 JSON `{ "error": "unauthenticated" }` for `/api/*` and server-action posts, clear a tampered cookie, respond 503 "Checkpoint has no access key configured" when no key is configured, exempt `/login`, `/api/session`, `/manifest.webmanifest` and `/icons/*`), `src/app/api/session/route.ts` (POST exchange setting an httpOnly, Secure, SameSite=Lax cookie and redirecting to `next` or `/`; DELETE clears it), `src/app/login/page.tsx` (labelled key field, "That key was not accepted", preserves `next`), and a "Sign out" control in `src/app/layout.tsx` on authenticated pages; write `src/lib/auth/session.test.ts` and `src/proxy.test.ts` covering unauthenticated page redirect, unauthenticated write 401, tampered cookie, expired cookie, correct key issues a session, wrong key refused, logout clears the session, and no configured key blocks login. Depends on 1.1.
- [x] 2.2 Write the change loader `src/lib/change/load.ts` with `listChanges(host, ref)` (inbox entries with name, ledger summary or missing or unparseable, task done and total, waves, decision state; ordered blocking first then alphabetical; `archive` excluded) and `loadChange(host, ref, name)` (artifact texts with per-file missing markers, delta specs tree, ledger rows and summary, risk and decision views, head sha and ref, a typed `not_found` for an unknown change); write `src/lib/change/load.test.ts` against the fixture adapter covering the two-change inbox ordering and status text, a missing ledger sorted as blocking, archive excluded, an empty listing, an unknown change, and counts computed from the artifacts. Depends on 1.2, 1.3, 1.4, 1.6.
- [x] 2.3 Write the write actions `src/lib/change/actions.ts` with `answerRow(host, {ref, change, id, answer, baseSha, today})` (refuse a blank answer, refuse a row that is not `needs_human` with "<id> is not waiting on a person", evidence per design D13, commit message per design D15, pass a conflict through with the fresh file) and `decide(host, {ref, change, state, note, baseSha, headSha, now})` (refuse approve while the ledger blocks with "ledger blocks approval", require a note for `returned`, merge via `withDecision`, commit message per design D15); add `src/app/changes/[name]/actions.ts` as the Next.js server actions wrapping both with a session re-check and `revalidatePath`; write `src/lib/change/actions.test.ts` covering answer flips the row with every other row byte-identical, a resolved row refused, approve writes the decision file with head sha and preserved request, send back writes the note, the server refusing an approve that bypasses the control, and a conflict returning the reloaded file. Depends on 1.2, 1.4, 1.6, 2.1.

## 3. Screens

Every task in this section takes layout, spacing, tokens and copy from `docs/checkpoint-design-brief.md` §Screens (task 1.8). When `docs/checkpoint-design-spec.md` exists (the comp transcription from `/design-comp-import`, design D21), that document wins over the brief for geometry and tokens; the spec scenarios win over both for copy and behaviour.

- [x] 3.1 Build the inbox: `src/app/page.tsx` (ref from `?ref=` or the default branch, header shows the ref, "No active changes on <ref>" when empty), `src/components/ChangeCard.tsx` (name, "N needs you" or "clear" or "ledger missing", done of total tasks, waves, decision state), `src/components/HostError.tsx` (failure kind text such as "GitHub rejected the token (unauthorized)", `retryAt` when present, retry control); write `src/app/page.test.tsx` and `src/components/ChangeCard.test.tsx` covering the listing with ledger status, missing ledger text, ref from query parameter, no active changes, and a host failure named on the inbox. Depends on 2.2, and on 1.8 for layout and tokens.
- [x] 3.2 Build the change page: `src/app/changes/[name]/page.tsx` (loads via `loadChange`, "No change named <name> on <ref>" with an inbox link, `HostError` on failure), `src/components/ChangeHeader.tsx` (name, ref, short head sha, "N of M tasks", "N waves", ledger summary "N needs you, N resolved, N invalid, of N rows", risk text for observed, stale and unobserved), `src/components/ArtifactTabs.tsx` (Proposal, Design, Tasks, Specs, Ledger in that order, `role="tablist"` with arrow-key navigation, active tab in the URL hash, "<file> is missing on <ref>" for an absent artifact, each delta spec under its capability path heading), `src/components/Markdown.tsx` (react-markdown with remark-gfm, no rehype-raw, links open in a new tab with `rel="noopener noreferrer"`); write `src/components/ChangeHeader.test.tsx`, `src/components/ArtifactTabs.test.tsx`, `src/components/Markdown.test.tsx` and `src/app/changes/[name]/page.test.tsx` covering tab order, raw HTML escaped, a missing design.md, two delta specs rendered, an unknown change, and a forbidden failure with retry time. Depends on 2.2, on 3.1 for `HostError`, and on 1.8 for layout and tokens.
- [x] 3.3 Build the ledger panel and answer form: `src/components/LedgerPanel.tsx` (every row with its class as a text label, invalid rows with their problems, the blocking reason), `src/components/AnswerForm.tsx` (option chips from `optionsFromQuestion`, free text field with a visible label, "An answer is required" on blank, submit to the server action with the shown `baseSha`, on conflict show "The ledger changed on <ref> since you opened it; reloaded" and re-render from the returned rows, move focus to the result message); write `src/components/LedgerPanel.test.tsx` and `src/components/AnswerForm.test.tsx` covering a blank answer refused, option chips offered, a successful answer re-rendering the row as resolved, and the conflict message with reloaded rows. Depends on 2.3, 3.2, and on 1.8 for layout and tokens.
- [x] 3.4 Build the decision bar `src/components/DecisionBar.tsx` (Approve with `aria-disabled` and its reason as text: "1 decision still needs you", "decisions.md is missing; the laptop side would refuse this", and the unparseable and invalid-row wordings; Send back with a required note "Say what should change"; the current decision with time and short head, a stale label "approved at <sha7>, branch has moved to <sha7>" that re-offers Approve; on conflict "The decision file changed on <ref>; reloaded"); write `src/components/DecisionBar.test.tsx` covering approve blocked by a needs_human row, approve blocked by a missing ledger, send back requiring a note, a stale approval after the branch moved, and the decision write conflict. Depends on 2.3, 3.2, and on 1.8 for layout and tokens.

## 4. End to end

- [x] 4.1 Write `e2e/checkpoint.spec.ts` on the fixture adapter at a 375 px viewport: sign in with the key, see two changes in the inbox, open `add-user-auth`, answer D1 by tapping a chip, see Approve become enabled, approve, and assert the written `decisions.md` and `checkpoint.json` through a test-only `src/app/api/fixture/file/route.ts` that responds only when `CHECKPOINT_GIT_HOST=fixture` and 404 otherwise, with `src/app/api/fixture/file/route.test.ts` asserting the 404. Depends on 3.3, 3.4.
