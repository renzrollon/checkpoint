# Decisions — reach-checkpoint-from-a-phone

| id | question | class | resolution | evidence |
|----|----------|-------|------------|----------|
| D1 | How do the post-login and unauthenticated redirects reach the address the phone used, when the server only knows its bind address? | agent_resolved | Relative `Location` (same-origin path, no scheme or host) built by hand in `src/lib/auth/redirect.ts`; `NextResponse.redirect` is dropped because it validates for an absolute URL | curl on 2026-09-05: `next dev -H 0.0.0.0` and `next start` both answer `location: http://0.0.0.0:<port>/` on `POST /api/session`, and `https://0.0.0.0:3300/changes/x` with `X-Forwarded-Host`; `node_modules/next/dist/server/web/spec-extension/response.js:98-109` (`validateURL`) |
| D2 | Should the `Secure` cookie rule change to make plain-http LAN testing work on a production build? | agent_resolved | No; the implemented rule (production or https sets `Secure`, development over plain http omits it) is written into the access-gate spec, and the README routes plain-http testing to the dev server and https testing to a tunnel or Vercel | `src/app/api/session/route.ts:20-22`; access-gate delta spec, scenario "the Secure attribute follows the transport" |
| D3 | How does the dev server admit a phone on the LAN without opening it to every origin? | agent_resolved | `allowedDevOrigins` from `CHECKPOINT_DEV_ORIGINS` (comma-separated, Next wildcard syntax), always prefixed with `127.0.0.1`; read by `next.config.ts` at startup, so not part of the zod env schema | dev-server 403 "Blocked cross-origin request" observed 2026-09-05 for `Origin: http://192.168.7.42:3200`; `node_modules/next/dist/server/lib/router-utils/block-cross-site-dev.js:77-107` |
| D4 | How are the change page's GitHub round trips reduced without changing the adapter contract or the error behaviour? | agent_resolved | `readSpecs` reads each directory level with `Promise.all`; `loadChange` runs the existence check concurrently with the reads under `Promise.allSettled` with fixed error precedence (existence `not_found`, then existence error, then first read error); output order unchanged | `src/lib/change/load.ts` `readSpecs` and `loadChange` (sequential `await` in a `for` loop); real change page measured at 5.9 s and 5.2 s against `renzrollon/interlock` on 2026-09-05 |
| D5 | Where does the owner learn how to get the app onto a phone today? | agent_resolved | A README section "Testing from your phone" with the LAN dev path, the https tunnel path on the production build, and the Vercel pointer, plus the `Secure` caveat; `.env.example` documents `CHECKPOINT_DEV_ORIGINS` | `README.md` "Running locally" and "Deploying to Vercel" sections; user request 2026-09-05 "I want to use and test this immediately" |
