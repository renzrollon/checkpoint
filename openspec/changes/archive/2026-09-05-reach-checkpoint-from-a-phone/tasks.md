## 1. Repro first: the redirect tests fail before any fix

- [x] 1.1 Rewrite the redirect assertions in `src/app/api/session/route.test.ts` and `src/proxy.test.ts` to the access-gate delta. Build requests whose URL host is `0.0.0.0:3200` (and one whose host is `localhost:3000`) carrying the header `host: 192.168.7.42:3200`, and one carrying `x-forwarded-proto: https` and `x-forwarded-host: checkpoint.example.app`. Session route: the configured key with `next=/changes/add-user-auth`, `next=/`, `next=/changes/x`, `next=//evil.example`, `next=https://evil.example/x`, `next=/login` and no `next` must answer 303 with `location` exactly `/changes/add-user-auth`, `/`, `/changes/x`, `/`, `/`, `/`, `/`; the cookie is still set on the same response. Proxy: unauthenticated `GET /changes/add-user-auth` and `GET /changes/add-user-auth?ref=feat/auth` must answer 307 with `location` exactly `/login?next=%2Fchanges%2Fadd-user-auth` and `/login?next=%2Fchanges%2Fadd-user-auth%3Fref%3Dfeat%2Fauth`; a tampered cookie is still cleared on that response. Every `location` assertion also checks the value contains no `://`. Replace the existing `${ORIGIN}/...` assertions with the path-only values. Add the Secure scenario: with `NODE_ENV` not `production`, a request URL of `http://0.0.0.0:3200/api/session` yields a `set-cookie` without `secure`, and `https://checkpoint.example/api/session` yields one with it. Acceptance: `npx vitest run src/app/api/session/route.test.ts src/proxy.test.ts` fails on the session-route `location` assertions before task 2.1 and on nothing else; record the failing assertion names in the task's commit note.

## 2. Fix the redirects and the development origins

- [x] 2.1 Add `src/lib/auth/redirect.ts` exporting `redirectToPath(path: string, status: 303 | 307): NextResponse` per design D1: refuse (throw) anything that does not start with a single `/` (so `//host`, `https://host`, `''` and `\\` are refused), set `location` to the path unchanged and `cache-control` to `no-store`, empty body. Use it in `src/app/api/session/route.ts` (303 with the output of `safeNextPath`; the session cookie is set on the returned response as today) and in `src/proxy.ts` (307 with the `/login?next=<pathname+search>` string it already builds; the tampered cookie is still deleted on the returned response); delete both `new URL(..., request.url)` and the `NextResponse.redirect` calls. Add `src/lib/auth/redirect.test.ts` covering a plain path, a path with a query string, `//evil.example`, `https://evil.example`, and an empty string. Acceptance: the 1.1 tests pass; `npm test`, `npm run lint` and `npm run typecheck` are green. Depends on 1.1.
- [x] 2.2 In `next.config.ts`, derive `allowedDevOrigins` from `process.env.CHECKPOINT_DEV_ORIGINS` per design D3: split on commas, trim, drop empties, always start with `127.0.0.1`, with a comment stating that the value is read once by the dev server at startup and is therefore not part of `src/lib/env.ts`. Add `CHECKPOINT_DEV_ORIGINS` to `.env.example` with a one-line explanation (development only, the phone's view of the Mac's hostname or IP, restart `npm run dev` after changing it). Acceptance, with the Bash sandbox off: `CHECKPOINT_DEV_ORIGINS=192.168.7.42 CHECKPOINT_GIT_HOST=fixture CHECKPOINT_ACCESS_KEY=dev npx next dev -H 0.0.0.0 -p 3200`, then a request for a `/_next/static/...` chunk named in the inbox HTML with `Host: 192.168.7.42:3200` and `Origin: http://192.168.7.42:3200` answers 200, and the same request with `Origin: http://192.168.9.9:3200` answers 403; with the variable unset the behaviour is identical to today (`127.0.0.1` allowed, everything else 403). Depends on nothing.

## 3. Read the change concurrently

- [x] 3.1 In `src/lib/change/load.ts`, make `readSpecs` read each directory level with `Promise.all` (recursing into subdirectories and reading `.md` files concurrently), flatten, and sort by path exactly as today; make `loadChange` start the existence `listDir` on the change directory together with the proposal read, `readCore` and `readSpecs`, wait with `Promise.allSettled`, and resolve with the precedence in design D4 (existence `not_found` wins and yields the typed `not_found`; any other existence error is thrown; otherwise the first rejected read in declaration order is thrown; otherwise the view is assembled unchanged). In `src/lib/change/load.test.ts` add: (a) a counting wrapper over the fixture host that records the maximum in-flight `readFile` and `listDir` calls, asserting both reach at least 2 for a temp change with three `spec.md` files under two capability directories, and that `change.specs` equals the sorted list of paths; (b) an unknown change on the fixture is still `{ kind: "not_found", name, ref }` with no error escaping; (c) a host whose existence `listDir` fails with kind `forbidden` while `readFile` fails with kind `upstream` surfaces the `forbidden` error; (d) a host whose existence check passes but whose spec `readFile` fails with kind `upstream` throws that error. Acceptance: `npm test` green with every existing `listChanges` and `loadChange` test unchanged. Depends on nothing.

## 4. Documentation and end-to-end proof

- [x] 4.1 Add the README section "Testing from your phone" after "Running locally", per design D5: the LAN development path (`CHECKPOINT_DEV_ORIGINS=<mac-ip> npm run dev -- -H 0.0.0.0`, find the address with `ipconfig getifaddr en0`, open `http://<mac-ip>:3000`, works with the fixture variables or the GitHub variables), the https tunnel path on the production build (`npm run build && npm start`, any https tunnel to `localhost:3000`, no origin allowlist involved), and a pointer to the existing Vercel section; a caveat that production cookies are `Secure`, so plain http on a LAN needs the development server; a note to restart `npm run dev` after changing the origins. Add `CHECKPOINT_DEV_ORIGINS` to the environment table as development-only. Add `.claude/memory/failure-modes/request-url-is-the-bind-host.md` (Next derives `request.url` from the bind host, so redirects must be relative) with an index line in `.claude/memory/MEMORY.md`. Depends on 2.2 for the variable name.
- [ ] 4.2 Prove it with the Bash sandbox off: `rm -rf .next && npm run build`; start `npx next start -H 0.0.0.0 -p 3300` with `CHECKPOINT_GIT_HOST=fixture CHECKPOINT_ACCESS_KEY=dev`; `curl -s -o /dev/null -D - -H "Host: 192.168.7.42:3300" -d "key=dev&next=/changes/x" http://127.0.0.1:3300/api/session` must show `location: /changes/x`; the same request with `-H "X-Forwarded-Proto: https" -H "X-Forwarded-Host: checkpoint.example.app"` must show `location: /changes/x`; an unauthenticated `GET /` with the same `Host` must show `location: /login?next=%2F`; then `npm run test:e2e` must pass. Paste the three `location` lines into the commit body. Depends on 2.1, 2.2, 3.1.
